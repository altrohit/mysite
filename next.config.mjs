/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/mysite',
  assetPrefix: '/mysite',
}

export default nextConfig
